{
  "name": "alexa-plus",
  "version": "0.1.0",
  "private": true,
  "description": "Alexa+ Smart Home Agent: MCP server over Streamable HTTP with a human-approval gate.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "main": "server/src/server.js",
  "scripts": {
    "start": "node server/src/server.js",
    "test": "jest --testPathPatterns=server/test",
    "lint": "eslint server"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "express": "^5.2.1",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "eslint": "^10.10.0",
    "jest": "^30.5.1",
    "supertest": "^7.2.2"
  }
}
